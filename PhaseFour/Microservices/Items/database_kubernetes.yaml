apiVersion: v1
kind: Namespace
metadata:
  name: retailproject-namespace
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pv-claim-item
  namespace: retailproject-namespace
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: retaildatabase-item-service
  namespace: retailproject-namespace
spec:
  selector:
    app: retaildatabase-items
  ports:
    - protocol: TCP
      port: 3306
      targetPort: 3306
      nodePort: 30008
  type: NodePort
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: retaildatabase-item-deployment
  namespace: retailproject-namespace
spec:
  replicas: 1
  selector:
    matchLabels:
      app: retaildatabase-items
  template:
    metadata:
      labels:
        app: retaildatabase-items
    spec:
      containers:
        - name: item-db-retaildatabase-container
          image: mysql:latest
          ports:
            - containerPort: 3306
          env:
            - name: MYSQL_ROOT_PASSWORD
              value: root
            - name: MYSQL_DATABASE
              value: dummy
          args: 
            - --character-set-server=utf8mb3
            - --collation-server=utf8mb3_general_ci
          volumeMounts:
            - name: mysql-persistent-storage-item
              mountPath: /var/lib/mysql
          resources:
            requests:
              cpu: 500m
              memory: 376Mi
            limits:
              cpu: 1000m
              memory: 752Mi
      volumes:
        - name: mysql-persistent-storage-item
          persistentVolumeClaim:
            claimName: mysql-pv-claim-item
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: retailproject-item-db-hpa
  namespace: retailproject-namespace
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: retaildatabase-item-deployment
  minReplicas: 1
  maxReplicas: 1
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 50