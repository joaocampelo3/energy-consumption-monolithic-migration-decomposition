version: '3.8'
#
# SERVICES
#
services:
  #
  # SERVICES
  #
  retailproject_service:
    build: retailproject
    container_name: ShippingOrderMicroserviceApp
    ports:
      - 8080:8080
    expose:
      - 8080